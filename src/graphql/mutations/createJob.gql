#import "../fragments/jobFragment.gql"
mutation createJob($inputTopic: String!, $outputTopic: String!,  $targetColumn: String!) {
  createJob: createAutomlJob(inputTopic: $inputTopic, outputTopic: $outputTopic, targetColumn: $targetColumn) {
    job {
        ...jobFragment
    }
  }
}
